target-version = "py313"

[lint]
# Small explicit set (ruff's built-in default is broader). E501 left out on purpose.
select = ["E4", "E7", "E9", "F", "W", "I", "UP", "B"]

[lint.per-file-ignores]
# Import must follow the sys.path insert that makes sibling scripts importable.
"research/scripts/export_records.py" = ["E402"]
